enum Tokens {
    CONTAINER = "container",

    MODEL = "model",

    SEMI_COLON = ";",
    CURLY_OPEN = "{",
    CURLY_CLOSE = "}",

    STRING = "string",
    INT = "int",
    IS = "is",
    NOT = "not",
    NULL = "null" | "sda",
}

export default Tokens;